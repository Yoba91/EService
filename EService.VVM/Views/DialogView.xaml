<Window x:Class="EService.VVM.Views.DialogView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EService.VVM.Views"
        mc:Ignorable="d"
        x:Name="Dialog"
        Title="{Binding Path=Title}" Height="200" Width="400" ResizeMode="NoResize">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="Message" Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="0" Text="{Binding Path=Message}" VerticalAlignment="Center" HorizontalAlignment="Center" TextWrapping="Wrap" Margin="5 5 5 5"/>
        <Button x:Name="Ok" Grid.Column="0" Grid.Row="1" Width="100" Height="30" Content="Ок" Command="{Binding Path=Command}" CommandParameter="{Binding ElementName=Dialog}"/>
        <Button x:Name="Cancel" Grid.Column="1" Grid.Row="1" Width="100" Height="30" Content="Отмена" Command="{Binding Path=Exit}" CommandParameter="{Binding ElementName=Dialog}"/>
    </Grid>
</Window>
